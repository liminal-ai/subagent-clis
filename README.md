# subagent-clis

Small CLIs that wrap coding-agent backends with a shared file contract and agent-ergonomic verbs.

## Packages

- [`cursor-sub`](packages/cursor-sub) — wraps local `cursor-agent`

## Development

```bash
pnpm install
pnpm -r build
pnpm -r test
```
